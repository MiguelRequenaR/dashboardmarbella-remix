
function AddSuministros({toggleModal}: {toggleModal: () => void}) {
  return (
    <div>
        Hola yo soy el modal :V
        <button onClick={toggleModal}>Cerrar</button>
    </div>
  )
}

export default AddSuministros